{
  "name": "personal-operating-system-site",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Public-link Property Management Command Center with an optional secure server-side OpenAI route.",
  "scripts": {
    "check": "node --check worker.js",
    "test": "node --test",
    "dev": "node dev-server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
