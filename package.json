{
  "name": "personal-operating-system-site",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Password-protected Property Management Command Center with a secure server-side OpenAI route.",
  "scripts": {
    "check": "node --check worker.js",
    "test": "node --test",
    "dev": "node dev-server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
